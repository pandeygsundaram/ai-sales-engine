# AI Sales Engine — Frontend

React + Vite + TypeScript + Tailwind dashboard for the AI Sales Engine.

## Development

```bash
npm install
npm run dev        # http://localhost:3000
npm run build      # production build
```

The frontend talks to the FastAPI backend at `http://localhost:8000`. See the root README for full setup instructions.
